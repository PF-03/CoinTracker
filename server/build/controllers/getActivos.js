"use strict";
//import axios from "axios";
//funcion para traer la info de la API
/*
async function getActivos ()  {
 
 const url = await axios(`https://api.coingecko.com/api/v3/coins/markets?vs_currency=usd&order=market_cap_desc&per_page=100&page=1&sparkline=false`);
 //console.log(url)
 const apiInfo = await url.data.map((e:any) => {
    return{
            symbol: e.symbol,
            name: e.name,
            image:e.image
    }
 })
 //console.log(apiInfo);
 return apiInfo;
 

}
getActivos();

export default getActivos;*/ 
